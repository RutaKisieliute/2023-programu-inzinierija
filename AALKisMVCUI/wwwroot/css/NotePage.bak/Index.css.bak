/* This has to be done so as the elemens of the note container layout
 * correctly */
body {
    display: flex;
    flex-flow: column;
}

/* So does this have to be done. */
body > div.container, body > div.container > main {
    flex:1 1 auto;
    height: calc(100% - 60px);
    position: relative;
    min-height: 400px;
}

body > footer {
    position: fixed !important;
    background-color: var(--bs-body-bg);
}

#main-note-container {
    display: flex;
    flex-direction: column;
    position: relative;
    height: 100%;
    width: 100%;
}

#main-note-container > * {
    margin: 10px 20px;
}

#main-clear-note-data {
    height: 40px;
    width: 160px;
}

#main-note-area {
    height: 100%;
    resize: none;
    outline: 0px solid;
    border: 2px solid;
    padding: 4px;
    background-clip: content-box;
    box-shadow: inset 0 0 0 2px lightGreen;
}

#main-note-area:focus {
    outline: 1px solid;
    border: 2px solid;
    box-shadow: inset 0 0 0 1px red; /* 1px here? Yeah, red stands out more, so it needs less width */
}
